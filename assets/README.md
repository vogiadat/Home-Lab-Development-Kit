# Assets

Store rendered images, icons, and other static assets used by the manual.

Rendered Mermaid diagrams are generated into:

```text
assets/images/
```

The generated PNG files are ignored by Git by default. They are produced by:

```bash
./scripts/render-diagrams.sh
```
